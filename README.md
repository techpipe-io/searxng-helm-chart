# searxng Helm repository

This repository contains a production-ready Helm chart for SearXNG with optional Valkey, Ingress, and Gateway API HTTPRoute support.

The chart itself lives in `charts/searxng`, refer to its [README.md](charts/searxng/README.md) for details. 
